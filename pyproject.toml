[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "llama.cpp"
version = "1.0.0"
description = "Package for llama.cpp"
requires-python = ">=3.8"
dependencies = []

[tool.setuptools.packages.find]
include = ["llama.cpp*"]
exclude = ["*"]
